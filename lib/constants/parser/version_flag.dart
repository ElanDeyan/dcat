/// Long name for the version flag.
const versionLongName = 'version';

/// Abbreviation for the version flag.
const versionAbbr = 'v';

/// Help message for the version flag.
const versionHelpMessage = 'Print the tool version.';
